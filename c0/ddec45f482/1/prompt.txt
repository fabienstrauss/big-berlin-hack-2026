Inspect the current codebase and report the minimal file structure and integration points needed to replace the mock /api/generate, /api/search, and /api/templates/apply handlers with provider-backed implementations while preserving the existing CanvasInsertionPayload contract. Focus on concrete file paths and any current assumptions in the client hook or contracts. Do not make changes.